name: Publish Magazine

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Upload magazine files
        run: |
          echo "Published at $(date +%Y-%m-%d)"
          ls -la
